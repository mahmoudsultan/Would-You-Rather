const LoadingBar = () => {
  return (<div className="flex flex-col h-screen w-screen md:w-3/4 mx-auto justify-center items-center">
    <h1 className="text-3xl md:text-6xl font-semibold">Hello There...</h1>
    <h2 className="mt-5 text-center text-xl md:text-3xl text-gray-600">Please hold on for a sec while I bring some pieces together</h2>
  </div>);
};

export default LoadingBar;
